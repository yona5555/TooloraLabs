export type { NotepadCalculatorOutput as NotepadResult, NotepadLineResult } from "@tooloralabs/tools";

export function defaultNotepadText(): string {
  return "Rent\nrent = 1200\nutilities = 85\ntotal = rent + utilities\nsplit between 3 roommates:\ntotal / 3";
}
