# تعليمات العمل على مشروع TooloraLabs

هذا الملف يحدد طريقة عملي (Claude) معك على هذا المشروع تحديدًا. اعتبره سياقًا دائمًا يُقرأ في بداية كل جلسة عمل.

## 0. المبدأ التوجيهي الأعلى: الاكتمال المطلق لكل أداة

هذا المبدأ يعلو كل القواعد التقنية الأخرى بهذا الملف، ويُطبَّق تلقائيًا على كل أداة حالية ومستقبلية دون تكرار الطلب.

- لا نبني أدوات "أساسية" أو "نموذجية بسيطة" أبدًا. كل أداة — بغض النظر عن بساطتها الظاهرية — يجب أن تكون كاملة وعميقة قدر الإمكان بما يخدم طبيعتها الخاصة تحديدًا، لا بما يُنقَل حرفيًا من أداة أخرى مختلفة تمامًا.
- الهدف: TooloraLabs منتج بمستوى شركة تقنية كبرى راسخة، لا موقع مبني بسرعة بمساعدة الذكاء الاصطناعي بحد أدنى من الجهد.
- **قبل البدء بأي أداة**، يُسأل أولاً: "ما الذي يجعل هذه الأداة تحديدًا أفضل أداة من نوعها على الإنترنت؟" — وكل ميزاتها تُبنى كإجابة على هذا السؤال، لا كنسخ لقائمة ميزات ثابتة من أداة أخرى.
- مثال: تعدد الوحدات/الأنظمة يُضاف فقط للأدوات التي تتعامل فعليًا مع قياسات جسدية أو مادية (BMI، العمر، الطول، الوزن). أدوات أخرى تحصل على عمقها الخاص المناسب لطبيعتها — مولّد كلمات المرور يحتاج فحص قوة متقدم وكشف تسريبات لا تحويل وحدات، وQR Code يحتاج أنواع محتوى متعددة وتخصيصًا بصريًا لا معادلات رياضية.
- عند تعميم قواعد §8 (معايير جودة المحتوى وSEO) على بقية الأدوات: الهيكل العام (Above the Fold، القسم الموسوعي، المراجع، خلف الأداة) يبقى ثابتًا للجميع، لكن **المحتوى الوظيفي التفصيلي** لقسم Above the Fold يُصمَّم بشكل مخصص لكل أداة حسب ما يجعلها الأفضل في فئتها تحديدًا — لا نسخة معدَّلة قليلًا من BMI.

## 1. تناسب حجم الفحص مع حجم المشكلة (2026-08-07)

هذه القاعدة تعلو أي قاعدة أخرى بهذا الملف فيما يخص أسلوب إصلاح المشاكل، وتُطبَّق تلقائيًا على كل مهمة قادمة دون تكرار الطلب.

- عندما تكون المشكلة المطلوب إصلاحها محددة وواضحة (وصف دقيق لعنصر بصري معيّن، خلل بسيط في مقاس أو مسافة أو لون، مكوّن غير مستخدم، أو ما شابه) — أذهب مباشرة إلى السبب الجذري الأكثر احتمالاً وأُصلحه فورًا، بأقل عدد ممكن من خطوات الفحص والتشخيص الإضافي. لا إطالة بقياسات أو استكشاف أو سكربتات تشخيصية معقدة إذا كان الحل المباشر والمنطقي واضحًا من وصف المشكلة نفسه.
- الفحص العميق والقياس الدقيق (قياس كل عنصر بالبكسل عبر `getBoundingClientRect`، أو الفحص التفاعلي الشامل بمتصفح حقيقي على عدة نقاط توقف) يُخصَّص فقط لحالتين فعليًا: (1) مشاكل تخطيط عامة غير محددة السبب مسبقًا، أو (2) التأكيد النهائي الشامل عند إعلان اكتمال مهمة كبيرة متعددة الخطوات تحتاج تحققًا نهائيًا.
- الهدف: حل المشاكل الصغيرة والمحددة بسرعة وبأقل تكلفة زمنية وموارد ممكنة، لا تطبيق نفس مستوى الفحص الشامل والمطوّل على كل تعديل بصرف النظر عن حجمه الفعلي مقابل حجم الجهد المبذول لحلّه.

## 2. طريقة التنفيذ: خطوة بخطوة

- لا تنفّذ مهام كبيرة دفعة واحدة. قسّم أي طلب إلى خطوات صغيرة قابلة للتحقق.
- بعد كل خطوة، تأكد أنها تعمل فعليًا قبل الانتقال للتالية (تشغيل، اختبار، أو قراءة النتيجة) — لا افتراض أن الكود يعمل لمجرد أنه "يبدو صحيحًا".
- إن كانت المهمة تحتاج قرارات معمارية أو فيها أكثر من مسار ممكن، اعرض الخيار المفضّل مع السبب باختصار بدل شرح كل الاحتمالات.

## 3. حل جذري لا ترقيع (Root cause, not patches)

- عند وجود خطأ أو مشكلة: ابحث عن السبب الجذري وأصلحه، لا تكتفِ بمعالجة العرض الظاهر.
- ممنوع الحلول المؤقتة (workarounds، try/catch لإخفاء الخطأ، تجاوز type errors، تعطيل اختبار فاشل) إلا إذا طُلب ذلك صراحة مع توضيح أنه حل مؤقت ولماذا.
- لا تضف طبقات تجريد أو معالجة أخطاء لسيناريوهات غير ممكنة الحدوث فعليًا في هذا الكود.

## 4. Commit وPush وتحقق CI بعد كل تغيير

بعد كل تغيير منطقي مكتمل (وليس بعد كل سطر):

1. تشغيل الفحوصات محليًا أولًا قبل أي commit، مطابقةً لما في `.github/workflows/ci.yml`:
   - `npm run lint --workspace=apps/web`
   - `npx tsc --noEmit --project packages/core/tsconfig.json`
   - `npx tsc --noEmit --project packages/sdk/tsconfig.json`
   - `npx tsc --noEmit --project packages/tools/tsconfig.json`
   - `npm run test --workspace=packages/core`
   - `npm run test --workspace=packages/sdk`
   - `npm run test --workspace=packages/tools`
   - `npm run build --workspace=apps/web`
2. عمل commit برسالة واضحة تشرح "لماذا" لا "ماذا" فقط.
3. Push إلى الفرع المناسب.
4. التحقق من نتيجة CI فعليًا (عبر `gh run watch` أو `gh run list`) وعدم افتراض النجاح — إن فشل، إصلاح السبب الجذري وإعادة الدورة كاملة (commit جديد، ليس تعديل السابق).

لا أطلب إذنًا لعمل commit/push إن كان التغيير جزءًا من مهمة صريحة طلبتَها؛ لكن لا أستخدم عمليات هدّامة (force push، reset --hard، تخطي hooks) إلا بطلب صريح منك في حينه.

**انتظار صامت تام لأي عملية طويلة (2026-08-07):** أثناء انتظار نتيجة CI، أو أي عملية أخرى تستغرق وقتًا (build طويل، تثبيت اعتماديات، عملية خلفية)، **ممنوع إرسال أي رسالة وسيطة إطلاقًا** — لا "لا جديد بعد"، ولا "أستمر بالمراقبة"، ولا أي تحديث تقدّم جزئي، بغض النظر عن طول مدة الانتظار أو عدد التحديثات الوسيطة الواردة من الأداة. الانتظار يكون صامتًا بالكامل حتى تكتمل العملية فعليًا (نجاحًا أو فشلًا)، ثم تُرسَل رسالة واحدة نهائية فقط تحمل النتيجة الكاملة مباشرة. هذه القاعدة تُطبَّق تلقائيًا على كل مهمة مستقبلية دون تكرار الطلب، وتَستثني فقط الحالات التي أحتاج فيها فعليًا اتخاذ قرار منك أثناء الانتظار (كسؤال توضيحي حقيقي)، لا للإبلاغ عن حالة الانتظار نفسها.

## 5. الإيجاز

- بلا شرح زائد أو مقدمات. جملة واحدة قبل أي تنفيذ توضح ما سأفعله، وتحديثات قصيرة عند نقاط مهمة فقط (اكتشاف شيء، تغيير اتجاه، عائق).
- في نهاية المهمة: سطر أو سطران فقط — ما الذي تغيّر وما التالي، بدون تلخيص مطوّل لما فعلته.
- لا تعليقات في الكود إلا إذا كانت تشرح سببًا غير واضح (قيد خفي، التفافة حول مشكلة معينة)؛ لا تعليقات تشرح "ماذا يفعل الكود".

## 6. بيئة العمل

- Claude Code يعمل مباشرة على هذا الجهاز عبر الطرفية، وله وصول مباشر لتنفيذ الأوامر، قراءة وكتابة الملفات، Git، وتشغيل الفحوصات — دون أي حاجة لنسخ/لصق من طرفك.
- بالتالي أي أمر أو فحص مذكور في هذا الملف (lint، typecheck، test، build، git commit/push) يُنفَّذ مباشرة بلا انتظار تدخل يدوي، إلا في الحالات التي تتطلب تسجيل دخول تفاعلي أو إذنًا صريحًا منك.

## 7. سياق المشروع

- Monorepo بـ npm workspaces: `apps/web` (Next.js 16)، `packages/core`، `packages/sdk`، `packages/tools`.
- الفرع الرئيسي: `main`.
- CI يعمل على push/PR إلى `main` (انظر `.github/workflows/ci.yml`).

## 8. معايير جودة المحتوى لصفحات الأدوات (SEO وأنظمة الذكاء الاصطناعي)

تُطبَّق هذه القواعد تلقائيًا على كل أداة حالية ومستقبلية دون الحاجة لتكرار الطلب. البدء الفعلي بتطبيقها كان على BMI Calculator كنموذج أولي (2026-08-02)، وتُعمَّم على بقية الأدوات بعد المراجعة.

**الهدف الاستراتيجي:** ليس فقط الترتيب الأول بجوجل، بل أن يصبح TooloraLabs مصدرًا موثوقًا تستشهد به محركات البحث (Google, Bing, DuckDuckGo, Brave, Yandex, You.com, Kagi) وأنظمة الذكاء الاصطناعي (ChatGPT, Claude, Gemini, Copilot, Perplexity, Meta AI, Grok, Google AI Overviews)، عبر الدقة والشفافية والمصداقية الحقيقية في كل معلومة — لا الحيل التقنية البحتة.

### 8.1 Above the Fold (بلا أي تمرير)

عند فتح أي صفحة أداة، تظهر فورًا 3 أعمدة:

- **يسار (~320px):** نموذج الإدخال، معبّأ بقيم افتراضية معقولة من البداية بحيث تظهر نتيجة فعلية فورًا دون أي تفاعل من الزائر.
- **وسط (~380–400px):** النتيجة، مع مؤشر بصري تفاعلي حقيقي (سهم/نقطة على SVG أو حساب موضع رياضي دقيق) يتحرك ديناميكيًا حسب النتيجة الفعلية — ممنوع أي شريط أو رسم ثابت لا يعكس المدخلات — بالإضافة لقائمة نقطية سريعة بأهم الأرقام المشتقة.
- **يمين (~300px):** أدوات ذات صلة + مساحة إعلان placeholder موحّدة (حدود منقطة، نص "Ad Space") تُبنى على مستوى القالب المشترك لتظهر تلقائيًا في كل الأدوات، لا يدويًا لكل أداة على حدة.

العمود الأيمن يختفي على الموبايل، والأعمدة الثلاثة تتكدس عموديًا. أي محتوى إضافي (شرح، جداول، رسومات، مراجع...) يظهر فقط أسفل هذا القسم.

### 8.2 جودة المحتوى

كل نص شرح بصياغة أصلية 100% (ممنوع نسخ أي نص حرفي من أي مصدر خارجي)، لكنه مبني على معلومات ونتائج حقيقية قابلة للتحقق (أرقام دقيقة، تصنيفات من جهات موثوقة كمنظمة الصحة العالمية عند الحاجة). ممنوع الحشو الإنشائي العام الذي لا يضيف معلومة فعلية.

### 8.3 القسم الموسوعي — نمط ورقة A4 علمية

كل المحتوى الإضافي (الشرح، الجداول، الرسومات، المعادلات، الأمثلة، الأسئلة الشائعة، خلف الأداة، المراجع) يُصمَّم بصريًا ليحاكي ورقة أكاديمية A4: خط serif، خلفية تشبه لون الورق، هوامش وتباعد أسطر أكاديمي، حدود خفيفة تحاكي حواف صفحة مطبوعة — منفصل بصريًا تمامًا عن قسم Above the Fold الحديث.

أي رسم توضيحي داخل هذا القسم: أبيض وأسود بالكامل (بدون ألوان)، SVG أصلي 100% (ممنوع النسخ أو التتبّع عن مصدر خارجي).

### 8.4 قسم "خلف الأداة" (إلزامي لكل أداة بلا استثناء)

حتى للأدوات التقنية البسيطة (QR Code، JSON Formatter، إلخ)، يتضمن:

1. **التاريخ والاختراع:** من اخترع/اكتشف المفهوم أو الأداة، متى، ولماذا (المشكلة التي حلّها وقتها).
2. **المعدات/المعايير التقنية الفعلية** المرتبطة (أجهزة مادية أو معايير/بروتوكولات تقنية رسمية).
3. **التطورات الحديثة** في هذا المجال اليوم.
4. **المسار الأكاديمي:** اسم التخصص الجامعي المرتبط، و5 جامعات مصنّفة فعليًا ضمن الأفضل عالميًا لهذا التخصص تحديدًا (حسب تصنيف موثوق مثل QS World University Rankings أو Times Higher Education، لا جامعات عشوائية)، + جامعة عربية واحدة أو اثنتين بنفس معايير الموثوقية إن وُجدت جامعة مناسبة. لكل جامعة: رابط رسمي مباشر، وتوضيح صريح هل تتيح دراسة هذا التخصص (أو برنامج مكافئ) أونلاين أم لا.

   **هذه المعلومات (التصنيف، الرابط، توفر الأونلاين) يجب التحقق منها فعليًا عبر بحث حقيقي على الإنترنت وقت الكتابة — لا من الذاكرة ولا تخمينًا.**

   **مراجعة دورية:** روابط الجامعات وتوفر البرامج أونلاين معلومات تتغيّر بمرور الوقت (تصنيفات تُحدَّث سنويًا، روابط تُعاد هيكلتها، برامج أونلاين تُطلَق أو تُلغى). عند أي تحديث مستقبلي على أي أداة، أو دوريًا كل بضعة أشهر إن لم يطرأ تحديث، يُعاد التحقق الفعلي (بحث حقيقي، لا افتراض بقاء الحالة كما وُثِّقت وقت الكتابة) من كل روابط الجامعات ومعلومات "خلف الأداة" لجميع الأدوات، لا الأداة قيد التعديل فقط.

### 8.5 المراجع العلمية

كل أداة تنتهي بقسم "المراجع العلمية" يستشهد بمصدر حقيقي موثوق (دراسة علمية، معيار تقني رسمي، أو جهة رسمية): الاسم الكامل، المصدر، السنة، ورقم DOI أو رابط رسمي — مع زر "اقرأ المصدر الأصلي" ينقل الزائر مباشرة للمصدر الحقيقي خارج الموقع. يُتحقَّق من صحة الرابط/DOI فعليًا قبل النشر.

### 8.6 خريطة الكلمات المفتاحية

قبل كتابة كل قسم، تُحدَّد 2-3 كلمات/عبارات بحث مستهدفة له تحديدًا، وتنعكس بشكل طبيعي غير متكلّف في عنوان القسم (H2/H3) وفقرته الأولى، بحيث تغطي الصفحة مجتمعة أكبر عدد من نوايا البحث الحقيقية للزائر (بحث سريع، بحث معلوماتي عميق، بحث أكاديمي، بحث عن مصادر).

### 8.7 سياسة مساحات الإعلانات

- **الصفحة الرئيسية (Home) تبقى خالية تمامًا من أي مساحة إعلانية، بلا استثناء أبدًا** — لا في Hero، ولا في Categories، ولا في أي قسم آخر منها. الهدف تجنّب انطباع أول تجاري بحت يرفع معدل مغادرة الزوار الجدد.
- الإعلانات تظهر فقط داخل صفحات الأدوات الفردية (`tools/[slug]`)، وبعد أن يكون الزائر قد بدأ فعليًا بالتفاعل مع الأداة واستفاد منها — لا قبل ذلك.
- مقاسان معياريان فقط بالموقع، كلاهما من `AdSpace` (`apps/web/components/tool-ui/AdSpace.tsx`) ولا يُبنى أي شكل ثالث يدويًا بأي مكان:
  - **`variant="sidebar"` (الافتراضي):** 300×600 بكسل (IAB Half Page)، بأبعاد ثابتة صريحة تمامًا (px لا نسبة مئوية) لتفادي أي Cumulative Layout Shift — يُستخدم فقط في أعمدة بعرض ثابت كالشريط الجانبي.
  - **`variant="leaderboard"`:** الاستثناء الوحيد المتعمَّد لقاعدة "px لا نسبة مئوية" — الارتفاع يبقى ثابتًا صراحة دائمًا (90px فسطح المكتب/تابلت من `sm:`، 50px قبلها) بينما العرض مرن (`w-full` بحد أقصى 728px فسطح المكتب أو 320px قبل `sm:`) ليتكيف مع أعمدة محتوى متفاوتة العرض (كحشو `EncyclopediaPaper` المتدرّج حسب نقطة التوقف) دون فيضان — نفس عطل الفيضان الذي أصاب صندوق الشريط الجانبي سابقًا حين كان مقاسه ثابتًا داخل عمود لا يتّسع له. يُستخدم للمساحات الإعلانية الأفقية العريضة داخل المحتوى (كالقسم الموسوعي)، لا في الشريط الجانبي.
  - كل استخدام لـ`AdSpace` — أيًا كان الـvariant — يعرض تلقائيًا تسمية شفافية صغيرة محايدة ("Sponsored"/"إعلان" عبر next-intl) أعلى الصندوق مباشرة، لا يدويًا لكل استدعاء.
  - **موضع `variant="leaderboard"` داخل القسم الموسوعي قرار تحريري يدوي لكل أداة**، وليس انعكاسًا تلقائيًا من `EncyclopediaPaper` نفسه — كل أداة تحدد موضعين مناسبين ضمن محتواها الخاص (كما في BMI: بعد قسم المخاطر الصحية، وبعد قسم الأمثلة المحلولة)، بتباعد كافٍ بينهما، لا موضعًا ثابتًا مفروضًا هيكليًا لعدم اختلاف بنية المحتوى بين الأدوات.

### 8.8 التصميم البصري الموحّد لقسم Above the Fold (ملحق، 2026-08-04)

هذه القواعد تكمّل §8.1 وتُطبَّق تلقائيًا على أي أداة above-the-fold (تُفعَّل بـ `contentWidth="wide"` و`compact` في `ToolPageLayout`) حالية أو مستقبلية، عبر ملفات القالب المشترك:

- **رأس الصفحة (`ToolPageLayout`, وضع `compact`):** سطر واحد فقط — أيقونة رجوع (`ArrowLeft` من lucide-react) + عنوان الأداة (`h1`) بمحاذاة أفقية، بلا شارة فئة وبلا وصف فرعي، وبأقل مسافة علوية/سفلية معقولة. هذا يعيش بالكامل في `ToolPageLayout.tsx` وينعكس تلقائيًا على أي أداة تستخدم `compact={true}`. أدوات `contentWidth="default"` (غير above-the-fold) تحتفظ بالرأس الكامل (شارة + وصف) دون أي تغيير.
- **الشبكة (`ToolAboveFold`):** `items-start` إلزامي على حاوية الـ grid — ممنوع ترك القيمة الافتراضية `stretch` تفرض تساوي ارتفاع عمودي input/result. كل صف يُحاذى من نقطة بدايته العلوية فقط، والارتفاع الطبيعي لكل بلوك يبقى كما هو.
- **البطاقة الموحّدة (`SectionCard`, في `tool-ui/SectionCard.tsx`):** أي بلوك في قسم above-the-fold (نموذج الإدخال، النتيجة، أدوات مصغّرة، توصيات، الشريط الجانبي، أو أي بلوك جديد كـ "ملاحظة صحية") **يجب** أن يُبنى بهذا المكوّن المشترك — حدود مستديرة، رأس أزرق (`bg-blue-600`) بعنوان أبيض bold، وجسم بحشو متجاوب. **هذا تطبيق يدوي لكل أداة، لا انعكاس تلقائي**: لا يوجد ملف قالب مشترك يمكنه حقن هذا التصميم في مكوّنات أداة موجودة (`AgeInputPanel`, `MortgageResult`, ...) دون تعديلها فعليًا لاستخدام `SectionCard` — عند بناء أو إعادة بناء أي بلوك above-the-fold لأي أداة، استخدام `SectionCard` إلزامي، لا اختياري.
- **ملء الفراغ الناتج عن عدم تساوي الارتفاعات:** بدل إعادة فرض التساوي، يُضاف بلوك محتوى حقيقي إضافي (غير مكرر لبلوكات أخرى في نفس الصفحة) في نفس عمود Flexbox القصير (بنفس نمط تكديس `<div className="flex flex-col gap-4">` المستخدم أصلًا لعمود `result`)، لا بحيلة CSS.
- **القسم الموسوعي (`EncyclopediaPaper`):** بلا حد عرض داخلي خاص به (`max-w-*`) — يمتد دائمًا ليطابق عرض حاوية `<main>` الأب (نفس عرض أعمدة above-the-fold مجتمعة على الأدوات الواسعة). التصميم الداخلي (ورق، serif، حدود) بلا تغيير.
- **الشريط الجانبي الموحّد (`RelatedToolsSidebar`, في `tool-ui/RelatedToolsSidebar.tsx`):** هذا — على عكس بقية `SectionCard` — مكوّن مشترك كامل (بحث + رابط "عرض الكل" + `AdSpace`) يُستدعى مباشرة بـ props (`currentSlug`, `category`) بلا أي نسخة يدوية لكل أداة؛ استبدال `AgeRelatedSidebar`/`MortgageRelatedSidebar` وأمثالها به إلزامي، لا نسخها. **لا تُعرض قائمة أدوات فردية بين شريط البحث وزر "عرض الكل"** (أُزيلت 2026-08-07) — المكوّن يقتصر على البحث ورابط الفئة فقط. فاصل بصري واضح (`border-t` + مسافة) قبل `AdSpace` دائمًا — لا التصاق بين المحتوى والإعلان.
- **قاعدة نقاط التوقف (breakpoints):** أي تكبير في الحشو/حجم الخط داخل above-the-fold (رأس الصفحة، `SectionCard`) يُفعَّل عند `lg:` (1024px) حصرًا، لا `sm:`/`md:` — لأن التخطيط نفسه لا يتحول لثلاثة أعمدة إلا عند `lg`، فبقاء المقاسات الكبيرة عند 768px (تابلت) يجعل المحتوى "حاسوبًا مصغّرًا" بدل تصميم متجاوب فعليًا.
- **التجاوب:** كل ما سبق يُختبَر فعليًا على 3 أحجام (كمبيوتر 1440px، تابلت 768px، هاتف 375px) قبل اعتباره منجزًا — لا افتراض أن `lg:`/`sm:` كافية بلا فحص بصري حي. (استثناء: هذا الفحص الشامل على 3 أحجام يخضع لقاعدة §1 — لا يُطبَّق على إصلاح محدد وواضح السبب، بل على مهام تخطيط عامة أو تأكيد اكتمال نهائي.)

## 9. الهوية البصرية الموحّدة للموقع (2026-08-04)

النمط المطبّق على BMI Calculator (رؤوس البلوكات الزرقاء `bg-blue-600`، بطاقات بحدود مستديرة، أزرار أساسية زرقاء) هو **الهوية البصرية الرسمية لكامل TooloraLabs** من الآن — لا تصميم خاص بأداة واحدة. تُطبَّق تلقائيًا دون تكرار الطلب على:

- كل أداة حالية ومستقبلية above-the-fold، عبر `SectionCard` و`RelatedToolsSidebar` (§8.8) — هذا الانعكاس مضمون على مستوى المكوّنات المشتركة فعليًا، لا نسخًا يدويًا.
- أي قسم جديد يُضاف لاحقًا above the fold أو غيره داخل صفحة أداة.
- أي صفحة أخرى بالموقع (الرئيسية، الفئات، القانونية) **بما يتناسب مع طبيعتها** — أي لا تُفرَض بطاقات برأس أزرق حرفيًا على صفحة نصية كالشروط والأحكام، لكن الأزرق نفسه (تدرّج `blue-600`/`blue-400`) يبقى هو لون العلامة الوحيد لأي عنصر تفاعلي أو تأكيدي (أزرار، روابط رئيسية، شارات) في كل الموقع دون استثناء.

أي عمل تصميمي مستقبلي (صفحة جديدة، قسم جديد، مكوّن جديد) يبدأ من هذه الهوية كنقطة انطلاق افتراضية، لا كخيار من بين عدة خيارات لونية.

## 10. اللغة العربية الفصحى إلزامية بكامل الموقع (2026-08-05)

كل نص عربي بالموقع (`apps/web/messages/ar.json` وأي محتوى عربي مستقبلي) يُكتب بالعربية الفصحى الواضحة والمهنية — **بلا أي لهجة محلية أو عامية**، أيًا كانت (خليجية، عراقية، مصرية، شامية، أو غيرها). تُطبَّق هذه القاعدة تلقائيًا على أي نص عربي جديد دون تكرار الطلب، بما في ذلك الصفحات القانونية.

- **المصطلحات التقنية** (BMI، DEXA، Stadiometer، وأمثالها) تبقى كما هي دون تعريب قسري إن لم يوجد مقابل عربي شائع ودقيق.
- **علامات اللهجة الشائعة الممنوعة** كمثال لا للحصر: "شنو" (الفصحى: ماذا)، "هذي/هالـ" (هذه)، "مو" (ليس)، "وايد" (كثيرًا)، "تبي/يبي" (تريد/يريد)، "لسا" بمعنى "ما زال" (لا يزال، حتى الآن)، "عندك" بصيغة السؤال المباشر غير الرسمية (لديك)، "لقيت" (وجدت)، "اللي" (التي/الذي)، "زي" (مثل)، "شي" (شيء)، "احنا" (نحن)، "راح" كأداة مستقبل (سـ/سوف)، وأي حرف أو صيغة صرفية خاصة باللهجات (چ، گ، إلخ).
- **قبل نشر أي نص عربي جديد أو معدَّل**، تُفحص الصياغة فعليًا (لا افتراضًا) للتأكد من خلوّها من هذه العلامات، مع مراعاة أن بعضها قد يظهر كجزء طبيعي من كلمة فصحى أخرى (مثل "اللياقة" أو "الساعات" أو "أونصة") وليس كل تطابق حرفي يعني وجود لهجة فعليًا.
- **هذه القاعدة عامة على كل صفحات الموقع** (الرئيسية، من نحن، تواصل معنا، الصفحات القانونية، وabove the fold والقسم الموسوعي لكل أداة) لا الصفحات القانونية فقط.
- النسخة الإنجليزية لا تتأثر بهذه القاعدة (لا مفهوم "لهجة عامية" بالإنجليزية بنفس المعنى)، وتبقى إنجليزية مهنية واضحة كما هي.

## 11. الحياد التام في كل أداة مالية (2026-08-08)

هذه القاعدة إلزامية لكل أداة مالية بالموقع — حالية (حاسبة الرهن العقاري) ومستقبلية (العملات الرقمية، العملات الدولية، السلع، وأي أداة مالية أخرى تُضاف لاحقًا) — دون الحاجة لتكرار الطلب لكل أداة على حدة.

- كل أداة مالية منصة معلوماتية وتعليمية محايدة بحتة، وليست منصة استشارة أو توصية مالية بأي شكل. الأداة تقدّم فقط: (أ) حساب/تحويل محايد تمامًا بلا أي حكم قيمي على النتيجة، (ب) معلومات توعوية عامة عن كيفية عمل المفهوم المالي، (ج) روابط لمصادر تعلّم خارجية لمن يريد التعمّق بنفسه، (د) روابط أخبار من مصادر خارجية دون أي تحليل أو رأي من الموقع نفسه.
- **ممنوع منعًا باتًا** أي صياغة توحي بتوصية أو نصيحة استثمارية شخصية أو عامة — أمثلة على الممنوع: "استثمر الآن"، "هذه فرصة جيدة"، "ننصحك بـ"، "الأفضل أن تشتري"، أو أي حكم على توقيت السوق أو جودة أصل معيّن كفرصة استثمارية.
- كل إشارة لأي منصة أو دورة أو أداة خارجية تُصاغ بحياد تام كمعلومة متاحة ("رابط لمصدر تعليمي"، "منصة تتيح كذا")، لا كتوصية أو دفع للشراء أو التسجيل أو الاستخدام.
- إخلاء المسؤولية المالي (متى وُجد في الأداة) يوضّح بصراحة أن المعلومات لأغراض توعوية فقط وليست نصيحة استثمارية، وأن الأسعار قد تتأخر عن السوق الفعلي، وأن استشارة مختص مالي مرخّص مطلوبة قبل أي قرار استثماري فعلي.
- **قبل نشر أي نص جديد أو معدَّل بأي أداة مالية**، يُفحص فعليًا (لا افتراضًا) للتأكد من خلوّه من أي صياغة توصية شخصية أو عامة، بما يشمل above the fold، القسم الموسوعي، الأسئلة الشائعة، وأي اقتراح أو تلميح ضمني.

## 12. البنية المعمارية: قاعدة الأدوات ذات البيانات الخارجية الحية (2026-08-08)

أي أداة تعتمد على جلب بيانات من مصدر خارجي حي (API خارجي، كأسعار عملات، طقس، مؤشرات اقتصادية) يجب استثناؤها صراحة من `generateStaticParams()` في `[locale]/tools/[slug]/page.tsx` منذ اللحظة الأولى لإنشائها — لا بعد اكتشاف مشكلة لاحقًا.

- **السبب:** `generateStaticParams()` تُجبر Next.js على تنفيذ prerender لتلك الصفحة وقت البناء (build time)، مما يعني استدعاء فعلي للـAPI الخارجي مع كل عملية `next build`، بغض النظر عن طبيعة التعديل المدفوع (حتى لو كان تعديلًا في أداة أخرى تمامًا). هذا يستنزف حصص الاستخدام المجانية المحدودة لمعظم الخدمات الخارجية بسرعة كبيرة، وقد يؤدي لفشل CI (خطأ 429 Rate Limit، كما حدث فعليًا مع CoinGecko في أداة العملات الرقمية بتاريخ 2026-08-08).
- **الحل الصحيح:** استبعاد صريح لتلك الأداة من قائمة `generateStaticParams()`، مع الاعتماد الكامل على `dynamicParams` الافتراضي (`true`) وخاصية `revalidate` الزمنية على استدعاءات `fetch` (مثل 3600 لكل ساعة) لتفعيل ISR حقيقي وقت التشغيل الفعلي فقط — الصفحة تُرندر عند أول طلب زائر حقيقي، لا عند البناء.
- **التحقق قبل اعتبار الإصلاح مكتملًا:** التأكد فعليًا (لا افتراضًا) أن `next build` لا يُنتج ملفات prerender لتلك الصفحة (`find .next/server/app` بعد بناء نظيف)، وأن أول طلب فعلي وقت التشغيل يجلب بيانات حية بنجاح، وأن الطلبات اللاحقة ضمن نافذة `revalidate` تُخدَم من الكاش دون إعادة جلب.
- **نطاق التطبيق:** هذه القاعدة تُطبَّق تلقائيًا وإلزاميًا على أي أداة مستقبلية تعتمد بيانات خارجية حية (Forex، السلع، World Bank، الطقس، فرق التوقيت، وأي أداة أخرى مشابهة لاحقًا) منذ لحظة إنشائها الأولى، دون انتظار اكتشاف نفس المشكلة من جديد.

## 13. سلوك SectionNav الافتراضي: التصاق مستمر حتى نهاية الصفحة (2026-08-09)

هذا السلوك معتمد رسميًا كجزء من مكوّن `SectionNav` المشترك (`apps/web/components/tool-ui/SectionNav.tsx`) نفسه، ويُطبَّق تلقائيًا على أي أداة تستخدمه — حالية (BMI، الرهن العقاري، العملات الرقمية) ومستقبلية — دون الحاجة لتكرار الطلب أو أي إعداد إضافي لكل أداة.

- **المشكلة الأصلية:** `position: sticky` يلتصق فقط ضمن حدود الحاوية الأب المباشرة للعنصر. بما أن `SectionNav` يُدرَج دائمًا داخل عمود "secondary" الضيق (بطاقات ما بعد Above the Fold)، وهذا العمود ينتهي قبل نهاية المحتوى الفعلي للصفحة (القسم الموسوعي، خلف الأداة، المراجع تعيش خارجه)، كان الشريط يتوقف عن الالتصاق قبل الوصول لنهاية الصفحة الحقيقية — بغض النظر عن الأداة.
- **الحل المعتمد:** `SectionNav` يراقب موضعه عبر `IntersectionObserver` على عنصر مؤشر (`sentinel`) خفي يسبقه مباشرة في DOM، ويتحول تلقائيًا من `position: relative` (ضمن التدفق الطبيعي) إلى `position: fixed` (مرتبط بحدود نافذة العرض فقط، لا بأي حاوية أب) بمجرد تجاوز نقطة الالتصاق — ويبقى كذلك حتى نهاية الصفحة الفعلية مهما كانت الحاوية الأب قصيرة. عنصر فراغ (spacer) بارتفاع مطابق يُدرَج تلقائيًا عند التفعيل لمنع أي قفزة بصرية بالمحتوى.
- **الأثر البصري:** عند الالتصاق، يمتد الشريط بعرض نافذة العرض كاملة (مطابقًا لعرض رأس الموقع)، بمحتوى داخلي محاذى بـ`max-w-6xl` كالمعتاد — لا يُقيَّد بعرض عمود ضيق محدد كما كان سابقًا.
- **التحقق المطلوب عند أي تعديل مستقبلي على هذا المكوّن:** فحص بصري فعلي بمتصفح حقيقي (تمرير حقيقي بعجلة الفأرة، لا `window.scrollTo` عبر JS مباشرة فقد يتأثر بتقييد المتصفح لتبويبات الخلفية) من أعلى كل صفحة تستخدم `SectionNav` حتى أسفلها تمامًا، على كل الأدوات التي تستخدمه، في كلا الوضعين الليلي والنهاري.

## 14. أولوية قصوى: الجمهور الدولي هو الأساس التجاري، لا الجمهور العربي (2026-08-10)

**هذه القاعدة بأولوية قصوى، تسبق أي قرار آخر متعلق بترتيب المحتوى الافتراضي في أي أداة مالية**، وتُطبَّق تلقائيًا وإلزاميًا على كل أداة مالية حالية ومستقبلية (العملات الرقمية، Forex، السلع، وأي أداة مالية أخرى تُضاف لاحقًا) دون الحاجة لتكرار هذا الطلب مرة أخرى أبدًا.

- **الجمهور المستهدف الأساسي لكل أداة مالية هو دولي/عالمي** — تحديدًا المتحدثون بالإنجليزية من دول ذات قيمة نقرة إعلانية مرتفعة في AdSense (أمريكا، بريطانيا، كندا، أستراليا، دول أوروبا الغربية)، لا جمهور عربي بالأساس. الجمهور العربي يمثّل نسبة صغيرة من الزوار (أقل من 10%) وليس الأولوية التجارية، رغم أهميته.
- **العربية تبقى لغة مدعومة بالكامل وبجودة كاملة** (فصحى احترافية، محتوى موسوعي كامل، بلا أي تقليص) — هذا التصحيح لا يمسّ جودة أو اكتمال الترجمة أو المحتوى العربي إطلاقًا. الأثر يقتصر تمامًا على **ترتيب الأولوية الافتراضية** لما يُعرض أولاً above the fold.
- **التطبيق العملي:** في كل أداة مالية (تحويل عملات، أسعار سلع، أو أي سوق مالي مستقبلي)، العملات/الأسواق الافتراضية المعروضة أولاً above the fold (القيمة الافتراضية عند فتح الصفحة، وترتيب أي قائمة "أهم العملات/الأسواق") يجب أن تكون الأعلى قيمة إعلانية وبحثًا عالميًا: **الدولار الأمريكي، اليورو، الجنيه الإسترليني، الين الياباني، الدولار الكندي، الدولار الأسترالي** — لا عملات عربية.
- العملات العربية (الريال السعودي، الدرهم الإماراتي، الجنيه المصري، الدينار الكويتي، وغيرها) **تبقى متاحة بالكامل** في أي قائمة بحث أو اختيار كامل لمن يبحث عنها تحديدًا، لكنها تُعرض **بعد** المجموعة الدولية أعلاه، لا كخيار افتراضي أول أو معروض في المقدمة.
- **مثال محدَّث (2026-08-10):** أداة العملات الرقمية كانت متوافقة أصلًا (الدولار افتراضي). أداتا Forex والسلع كانتا تستخدمان الريال السعودي كعملة افتراضية ثانية/وحيدة above the fold — صُحِّح هذا لاستخدام اليورو (Forex) والدولار الأمريكي (السلع) كافتراضي، مع إعادة ترتيب قوائم العملات البارزة (`FEATURED_CURRENCY_CODES` في Forex) لتضع المجموعة الدولية أولًا.
- **نطاق التطبيق المستقبلي:** أي أداة مالية جديدة تُبنى لاحقًا تبدأ من هذه الأولوية كنقطة انطلاق افتراضية منذ لحظة تصميمها الأولى — لا كخيار من بين عدة خيارات، ولا حاجة لتذكير بهذا مستقبلًا.

## 15. قاعدة معمارية دائمة: كل صفحة أداة قابلة للتوسع بلا إعادة هيكلة (2026-08-10)

هذه القاعدة معمارية عامة، تُطبَّق تلقائيًا على كل أداة حالية ومستقبلية بالموقع بلا استثناء، دون الحاجة لتكرار الطلب.

- **المبدأ:** كل صفحة أداة تُبنى بحيث يمكن إضافة أقسام جديدة (above the fold أو في القسم الموسوعي) بسهولة مستقبلاً كعناصر إضافية في القالب المشترك (`SectionNav` و`EncyclopediaPaper`)، دون الحاجة لإعادة هيكلة الصفحة من الصفر في كل مرة.
- **التطبيق العملي:** إضافة قسم جديد above the fold تعني فقط: (1) بناء مكوّن جديد مستقل لذلك القسم، (2) إدراجه ضمن عمود `secondary` في `ToolAboveFold` (أو ضمن عمودَي `input`/`result` عبر `<div className="flex flex-col gap-4">` إن كان مرتبطًا بهما مباشرة)، (3) إضافة عنصر جديد لمصفوفة `navItems` الممرَّرة إلى `SectionNav` بمعرّف `id` مطابق لمعرّف القسم. إضافة قسم جديد بالقسم الموسوعي تعني فقط: بناء مكوّن `InfoSection` جديد وإدراجه داخل `EncyclopediaPaper`. لا حاجة لأي تعديل هيكلي آخر على الصفحة أو المكوّنات القائمة.
- **حالة الالتزام الحالية (تحقّق فعلي بتاريخ 2026-08-10):** جميع الأدوات above-the-fold القائمة (BMI، العمر، الرهن العقاري، العملات الرقمية، Forex، السلع) تتبع هذا النمط فعليًا بالفعل — كل قسم إضافي (كقسم "الأجهزة والمعدات" التوعوي عبر `EquipmentSection` المشترك في BMI، أو قسم الرسم البياني التاريخي في العملات الرقمية/Forex/السلع) مبني كمكوّن مستقل مُدرَج ضمن هذا القالب، لا كجزء متشابك من صفحة أحادية الكتلة.
- **الفائدة:** هذا يضمن أن أي توسّع مستقبلي لأي أداة (كمقارنة بين مدن أو سجل تاريخي إضافي بقسم الطقس مثلاً) يبقى تعديلاً إضافيًا محدود النطاق (مكوّن جديد + عنصر تنقّل جديد)، لا مشروع إعادة هيكلة كامل للصفحة.

## 16. أولوية قصوى: ممنوع ترك أي commit محلي بلا push (2026-08-21)

**هذه القاعدة بأولوية قصوى، وتُعزِّز §4 بقيد إلزامي إضافي صريح**، وتُطبَّق تلقائيًا وبلا استثناء في كل جلسة عمل مستقبلية على هذا المشروع، على أي جهاز، دون الحاجة لتكرار هذا الطلب مرة أخرى أبدًا.

- **الحادثة التي أوجبت هذه القاعدة:** بتاريخ 2026-08-21، تراكمت 73 commit محليًا (ترجمات i18n لعدة أدوات) دون أي push إلى `origin/main` لفترة طويلة، مما عرّض كل هذا العمل المكتمل فعليًا لخطر ضياع حقيقي عند أي مشكلة بالجهاز (تلف، سرقة، فورمات، إلخ) — لأن نسخته الوحيدة كانت محلية بحتة.
- **القاعدة:** أي عمل يكتمل فعليًا ويُحفظ بـcommit محلي — بعد كل commit واحد، أو بعد كل مجموعة commits منطقية مترابطة إن كانت المهمة تتطلب عدة commits صغيرة متتالية لإكمال وحدة عمل واحدة — **يُدفع (`git push`) إلى `origin/main` فورًا**، لا لاحقًا، ولا بعد تجميع عدة مهام مختلفة معًا.
- **ممنوع إطلاقًا** ترك عمل مكتمل (commit تم إنشاؤه فعليًا) بلا push لفترة طويلة أو للانتظار حتى "نهاية الجلسة" أو "تجميع عدة أدوات معًا" قبل الدفع — كل ما هو مكتمل (test/build ناجح محليًا وcommit تم) يُدفع بأقرب فرصة عملية ضمن نفس الجلسة.
- **بعد كل push**، يُتحقَّق فعليًا (لا افتراضًا) من نجاح تشغيل CI المرتبط عبر `gh run watch` أو `gh run list` (كما في §4) — لا الانتقال للمهمة التالية قبل هذا التحقق.
- **ملاحظة تقنية مهمة:** GitHub Actions يُشغِّل CI مرة واحدة لكل عملية `push` (على الـHEAD النهائي المدفوع)، لا مرة منفصلة لكل commit داخل نفس الـpush. لذلك الدفع المتكرر والمتقارب (commit ثم push فورًا) هو ما يضمن فعليًا تحقق CI الفعلي والمنفصل لكل وحدة عمل — لا الدفع الجماعي المؤجَّل الذي يختزل عشرات الـcommits في تشغيل CI واحد فقط في النهاية.
- **نطاق التطبيق:** هذه القاعدة تسري على كل نوع عمل (i18n، إصلاحات، ميزات جديدة، تعديلات محتوى) وعلى أي جهاز يُستخدم لهذا المشروع مستقبلاً، لا هذا الجهاز فقط.

## 17. قاعدة معمارية دائمة: ممنوع ترك أي فراغ بصري كبير غير مستغل — يُملأ بجدول مرجعي (2026-09-07)

هذه القاعدة تُطبَّق تلقائيًا على كل أداة حالية ومستقبلية above-the-fold بلا استثناء، دون الحاجة لتكرار الطلب.

- **المشكلة الأصلية:** `ToolAboveFold` (§8.8، الفقرة الأخيرة) يتعامل مع فارق الارتفاع بين عمودَي `input`/`result` تلقائيًا: عندما يكون عمود `result` أطول (بسبب رسومات/مؤشرات/تبويبات/بطاقة مكافأة متعددة)، تُمدَّد حاوية `input` لمطابقة ذلك الارتفاع ويصبح محتواها `sticky` — لكن هذا يترك فراغًا بصريًا فارغًا وواضحًا أسفل بطاقة الإدخال (أسفل زرَي Calculate/Clear مباشرة) حتى يبدأ التمرير، كما ظهر فعليًا في لقطة شاشة لأداة pH Calculator.
- **القاعدة:** أي فراغ بصري كبير من هذا النوع (خصوصًا أسفل عمود المدخلات) **يُملأ دائمًا** ببطاقة `SectionCard` تحتوي جدولًا مرجعيًا/حسابيًا حقيقيًا وذا صلة مباشرة بموضوع الأداة تحديدًا — لا يُترك فارغًا أبدًا، ولا يُعالَج بإعادة فرض تساوي الارتفاعات (هذا يطبّق نفس مبدأ §8.8 "ملء الفراغ بمحتوى حقيقي إضافي"، لكن يحدد بدقة **نوع** المحتوى المطلوب: جدول مرجعي، لا أي بلوك عام).
- **المكوّن المشترك:** `apps/web/components/tool-ui/ReferenceTableCard.tsx` — بطاقة عامة تأخذ `title`/`caption`/`columnLabel`/`columnValue`/`rows` (كل صف: `label` + `value` + `tag` اختياري للتصنيف كالحمضي/القاعدي). يُستدعى من مكوّن `<ToolName>ReferenceTable.tsx` خاص بكل أداة (يحمل البيانات الثابتة والترجمات)، لا نسخًا يدويًا لهيكل الجدول HTML في كل أداة.
- **التطبيق العملي:** المكوّن الجديد يُدرَج داخل عمود `input` نفسه (`<div className="flex flex-col gap-3"><XInputPanel/><XReferenceTable/></div>`)، بنفس نمط تكديس Flexbox المستخدم أصلًا في عمود `result` — لا أي تعديل على `ToolAboveFold` نفسه.
- **معايير محتوى الجدول:**
  - بيانات حقيقية وموثقة (كثافات مواد، قيم pH، تركيزات معملية قياسية، ثوابت فيزيائية/كيميائية، سرعات أو قوى واقعية) — **ممنوع اختلاق أرقام تقريبية بلا مصدر منطقي**.
  - ذو صلة مباشرة بموضوع الأداة تحديدًا، لا محتوى عام قابل للنسخ بين الأدوات.
  - 5-6 صفوف عادةً، منسّق كبطاقة (`SectionCard`) بنفس أسلوب باقي البطاقات (رأس أزرق، حدود، تباعد سخي).
  - الجدول بأكمله يُغلَّف بـ`dir="ltr"` (مطابقًا للنمط المستخدم فعليًا بجداول `PhQuickReference`/`MolarMassResult`) حتى مع تسميات معرَّبة، لأن الأعمدة والأرقام تبقى محاذاة LTR بغض النظر عن لغة النص داخلها.
- **حالة التطبيق الأولى (تحقّق فعلي بتاريخ 2026-09-07):** طُبِّقت على كل الاثني عشر أداة من دفعتَي الفيزياء والكيمياء (Density، Ohm's Law، Energy/Work/Power، Kinematics، Force، Projectile Motion، pH، Molarity، Ideal Gas Law، Chemical Equation Balancer، Stoichiometry، Molar Mass) — كل أداة حصلت على جدول مخصص لموضوعها (مثال: كثافات مواد لـDensity، قيم pH شائعة لـpH، تركيزات معملية لـMolarity، ثوابت القياس المولي لـStoichiometry).
- **نطاق التطبيق المستقبلي:** عند بناء أي أداة جديدة above-the-fold، أو تحسين أداة قديمة قائمة بهذا النمط، **يُفحص فعليًا** (لا افتراضًا) وجود فراغ مشابه أسفل عمود المدخلات (أو أي عمود آخر بفارق ارتفاع ملحوظ)، ويُضاف الجدول المناسب من البداية — لا ينتظر طلبًا صريحًا لكل أداة على حدة.

## 18. قاعدة معمارية دائمة: "جزيرة عميل" لمؤشرات تفاعلية داخل القسم الموسوعي الثابت (2026-09-07)

هذه القاعدة معتمدة رسميًا كنمط معماري دائم، تُطبَّق تلقائيًا على أي مؤشر تفاعلي (gauge حي، شريط تقدّم، slider، محوّل مباشر، إلخ) يُضاف مستقبلًا داخل القسم الموسوعي (`*Education.tsx` / `EncyclopediaPaper`) لأي أداة حالية أو مستقبلية، دون الحاجة لتكرار الطلب.

- **السياق:** ملفات `*Education.tsx` (القسم الموسوعي) هي مكوّنات خادم بالكامل (`async function`, لا `"use client"`) — تُبنى بقيم مثال ثابتة مُمرَّرة كـ props حرفية وقت الرندر، لا تتفاعل مع أي حالة عميل. هذا يناسب الرسوم التوضيحية الثابتة الخمسة (أو أي عدد) الموجودة أصلًا بكل أداة، لكنه لا يسمح بمؤشر يستجيب فعليًا لنقر/سحب/كتابة الزائر.
- **الحل المعتمد:** أي مؤشر تفاعلي يُبنى كمكوّن مستقل خاص به يحمل `"use client"` و`useState` الخاص به (يشبه بنية بطاقات المكافأة above-the-fold)، ثم يُستورد ويُدرَج مباشرة داخل مكوّن `*Education.tsx` الخادمي — Next.js App Router يسمح بهذا التداخل (خادم يستورد عميل) بلا أي قيد، فيتحول ذلك الجزء فقط من الصفحة الثابتة إلى "جزيرة" تفاعلية، بينما تبقى بقية الصفحة (النص، الرسوم الثابتة) مُصيَّرة على الخادم كما هي.
- **الغلاف البصري الموحّد:** `apps/web/components/tool-ui/EncyclopediaLiveWidget.tsx` — غلاف عرضي عام (لا حالة، لا `"use client"`) يأخذ `title`/`children`، ويُستخدم داخل كل مؤشر تفاعلي جديد لضمان تناسق بصري واحد عبر كل الأدوات: حدود شفافة متوافقة مع تصميم "الورقة الأكاديمية" (`border-current/20`)، تسمية زرقاء صغيرة أعلى الصندوق (`text-blue-600 dark:text-blue-400`) هي العلامة الوحيدة التي تُميّز هذا الصندوق كـ"تفاعلي حي" عن النص الثابت المحيط به — لا رأس أزرق كامل كبطاقات `SectionCard` above-the-fold (يبقى ذلك الطراز حصريًا لقسم Above the Fold).
- **بيانات المؤشر:** يجب أن يتفاعل مع بيانات حقيقية فعليًا — إما مدخل حر يكتبه الزائر (كمحوّل الوحدات)، أو اختيار من قائمة قيم حقيقية موثقة (كمقياس الطفو/الغرق حسب المادة)، أو متغيّر مستمر حقيقي (كشريط الحرارة مقابل كثافة الماء) — لا عنصر ديكوري بلا وظيفة فعلية.
- **حالة التطبيق الأولى (تحقّق فعلي بتاريخ 2026-09-07، أداة Density Calculator):** 3 مؤشرات — مقياس الكثافة النوعية التفاعلي (طفو/غرق حسب مادة مختارة، في قسم "Understanding Density")، محوّل وحدات حي بإدخال حر (في "Converting Between Units" ضمن Behind the Tool)، ومنحنى كثافة الماء التفاعلي حسب الحرارة (في قسم الأسئلة الشائعة) — كل واحد بموقع مختلف، وظيفة مختلفة، ومصدر بيانات حقيقي مختلف.
- **التحقق البصري الإلزامي:** بما أن هذه الإضافات تفاعلية فعليًا (وليست نصًا أو رسمًا ثابتًا)، يُفحص عملها فعليًا بمتصفح حقيقي (لا افتراضًا من الكود فقط) بعد كل تعديل عليها — انظر §19 لطريقة الفحص البصري الفعلي المعتمدة بهذا المشروع.
- **نطاق التطبيق المستقبلي:** أي مؤشر تفاعلي جديد بأي قسم موسوعي لأي أداة (حالية أو مستقبلية) يتبع هذا النمط تلقائيًا من التصميم الأول — جزيرة عميل مستقلة + `EncyclopediaLiveWidget` كغلاف — لا حاجة لتصميم نمط جديد أو طلب إذن بهذا الخصوص مجددًا.

## 19. الفحص البصري الفعلي: Playwright عبر متصفح Brave المثبَّت محليًا (2026-09-07)

هذه القاعدة تُطبَّق تلقائيًا في كل مهمة مستقبلية تتطلب تحققًا بصريًا فعليًا (لا افتراضًا من الكود) — إصلاح خلل بصري، تراكب، تخطيط، أو أي مؤشر/مكوّن جديد — دون الحاجة لتكرار الطلب أو إعادة اكتشاف الحل من الصفر.

- **الأداة المعتمدة:** متصفح **Brave** المثبَّت مسبقًا على الجهاز كـsnap (المسار الفعلي: `/snap/brave/676/opt/brave.com/brave/brave` — وليس `/snap/bin/brave`، وهو رابط رمزي لغلاف snap لا يصلح مباشرة لتشغيل headless عبر Playwright)، يُشغَّل بوضع headless عبر حزمة `playwright` (تُثبَّت محليًا وقت الحاجة عبر `npm install playwright` في مجلد scratchpad المؤقت، مع تمرير `executablePath` لمسار Brave أعلاه بدل تحميل Chromium منفصل عبر `npx playwright install`).
- **لماذا Brave لا Chromium منفصل:** تجنّب تحميل متصفح إضافي بحجم كبير (~300 ميجابايت) حين يوجد متصفح فعلي مثبَّت أصلًا على الجهاز — وهذا ما طلبه المستخدم صراحة (2026-09-07) بعد أن بدأ Claude بتحميل Chromium افتراضيًا عبر Playwright.
- **آلية الفحص:**
  1. التأكد أولًا أن خادم التطوير (`npm run dev`) يستجيب فعليًا على `localhost:3000` (عبر `curl`) قبل أي محاولة تصوير.
  2. سكربت Node صغير في scratchpad يفتح الصفحة عبر Brave headless، بانتظار `waitUntil: "load"` (لا `"networkidle"` — خوادم Next.js dev تُبقي اتصال WebSocket لإعادة التحميل الحي (HMR) مفتوحًا باستمرار، مما يجعل `"networkidle"` يتوقف بانتهاء المهلة دائمًا في وضع dev تحديدًا).
  3. لقطة كاملة الصفحة (`fullPage: true`) وليس لقطة عنصر مفرد (`elementHandle.screenshot()`) عند الحاجة لتقييم تراكب أو تخطيط عام — عناصر `position: fixed`/`sticky` (كشريط التنقّل اللاصق) تتكرر بصريًا في كل شريحة من التقاط عنصر طويل يتطلب تمريرًا داخليًا للالتقاط، منتجةً تراكبًا وهميًا غير موجود فعليًا لزائر حقيقي — هذا فخ تشخيصي فعلي وقع فيه الفحص الأول لهذه الأداة، صُحِّح بمقارنة لقطة العنصر المفرد بلقطة الصفحة الكاملة النظيفة.
  4. لتحديد إحداثيات القص الدقيقة لمنطقة معيّنة: استخدام `page.evaluate` لاستخراج `getBoundingClientRect().top + window.scrollY` للعناصر المستهدفة (عناوين `h3`، عناصر `svg`، إلخ) **ضمن نفس تشغيل السكربت** الذي أخذ اللقطة (لا تشغيل منفصل لاحق) — لأن حالة الصفحة (ترتيب/ارتفاع Ad Space placeholder، حالة توسيع الأكورديون) قد تختلف طفيفًا بين تشغيلين منفصلين لنفس الصفحة، مما يُنتج إحداثيات غير متطابقة مع اللقطة الفعلية.
  5. الوضع الداكن يُفعَّل فعليًا (لا بافتراض CSS media query) عبر `page.evaluate(() => localStorage.setItem("theme", "dark"))` ثم `page.reload()` — يطابق آلية `ThemeInitScript.tsx` الفعلية بالموقع (قراءة `localStorage.getItem("theme")`، لا `prefers-color-scheme` وحدها).
- **نطاق التطبيق:** هذه الآلية تُستخدم تلقائيًا لكل تحقق بصري مستقبلي بهذا المشروع، على أي أداة، دون الحاجة لإعادة شرح الطريقة أو تبرير استخدام متصفح حقيقي بدل التخمين من الكود فقط.
- **فخ إضافي مؤكد وقوعه فعليًا (2026-09-07):** فحص "عدم وجود مفتاح ترجمة خام" عبر `curl` + `grep` على HTML الخام **غير موثوق** — صفحات Next.js التي تحوي مكوّنات عميل (`"use client"`) تضمّن كامل قاموس الترجمة (بصيغة JSON، بما فيها كل نصوص `{placeholder}` غير المُفسَّرة حرفيًا) داخل الصفحة لأغراض الـhydration، فيطابقها `grep` خطأً كأنها نص ظاهر للزائر. الفحص الصحيح: عبر Playwright، استخراج `textContent` الفعلي للعناصر المرئية المستهدفة تحديدًا (`page.evaluate(() => document.querySelector(...).textContent)`)، لا تفتيش HTML الخام بأكمله.

## 20. قاعدة معمارية دائمة: عناصر اختيار سريع لتنويع حسابي حي — في كل مكان مناسب بالأداة، لا القسم الموسوعي فقط (2026-09-07)

هذه القاعدة تُطبَّق تلقائيًا على كل أداة حالية ومستقبلية، في كل موضع مناسب لطبيعتها — بما يشمل صراحة لوحة الحاسبة الأساسية (above-the-fold) نفسها، لا القسم الموسوعي فقط — دون الحاجة لتكرار الطلب.

- **المبدأ:** أي مكان بالأداة يمكن أن يستفيد من تنويع حسابي حي عبر اختيارات جاهزة (أزرار/رقاقات قابلة للنقر) يُضاف له هذا العنصر — لا يُترك الزائر مضطرًا لكتابة أرقامه الخاصة فقط ليرى تنوعًا بالنتائج.
- **المرجع الناجح المعتمد كنموذج:** مؤشر SG Gauge بأداة Density Calculator — النقر على مادة (Air/Ice/.../Osmium) يُحدِّث المؤشر والجملة الحية المصاحبة فورًا وفعليًا، بلا حاجة لكتابة أي رقم يدويًا.
- **نطاق التطبيق العملي:** هذا لا يقتصر على القسم الموسوعي (`*Education.tsx`، عبر نمط "جزيرة العميل" في §18) — يشمل أيضًا لوحة الإدخال الأساسية above-the-fold نفسها حيثما يناسب منطق الأداة. مثال مطبَّق فعليًا: رقاقات مواد شائعة (Air/Ice/Water/Aluminum/Iron/Gold) في لوحة "Known Values" الخاصة بـDensity Calculator، تملأ حقلي الإدخال النشطَين حسب وضع solve-for الحالي بقيم واقعية لتلك المادة (حجم 100 سم³ مرجعي + الكتلة أو الكثافة المطابقة فعليًا)، ثم تُعيد الحساب وتُحدِّث النتيجة فورًا (بنفس آلية `onOperationChange` الفورية، لا انتظار ضغط Calculate).
- **معيار "التنويع الحسابي الحقيقي":** كل اختيار يجب أن يمثّل قيمة أو سيناريو حقيقيًا وموثقًا (مادة فعلية، قوة واقعية، مثال محسوب بأرقام صحيحة) لا قيمًا عشوائية أو زخرفية، ويُحدِّث النتيجة الفعلية حيًا فور النقر — لا يكتفي بتعبئة الحقول وانتظار إجراء إضافي من الزائر.
- **نطاق التطبيق المستقبلي:** عند بناء أو تحسين أي أداة، يُسأل: "أين يمكن لزائر أن يستفيد من تجربة قيمة جاهزة بنقرة واحدة بدل الكتابة اليدوية؟" — above the fold وضمن القسم الموسوعي كليهما، لا مكان واحد افتراضي.

## 21. قاعدة معمارية دائمة: عمق وحجم ونظافة نسخة PDF/الطباعة (2026-09-07)

هذه القاعدة تُطبَّق تلقائيًا على كل أداة حالية ومستقبلية عبر الملفات المشتركة أدناه — لا حاجة لتكرار الطلب لكل أداة.

- **الملفات المشتركة الفعلية:** `apps/web/lib/pdf/generateToolPdf.ts` و`apps/web/lib/pdf/gauge.ts` (مسار "Download PDF" عبر jsPDF+html2canvas)، و`apps/web/app/globals.css` (قاعدة `@media print` المسؤولة عن مسار "Print" عبر `window.print()` — مشتركة فعليًا عبر كل الأدوات، بعكس بنية `[data-print-area]` نفسها التي تبقى نسخة يدوية داخل كل `*ShareExportModal.tsx`).
- **معيار العمق:** لا يُكتفى بجدولي Inputs/Results مجردين. كل PDF/طباعة يتضمن: (أ) الجداول، (ب) **جملة "طريقة الحساب"** — تُعاد استخدام `sentence` الموجودة أصلًا كـprop بكل `*ShareExportModal.tsx` (تُبنى أصلًا للعرض on-page وللمشاركة النصية) بدل تأليف نص جديد، عبر تمريرها إلى `generateToolPdf({ sentence, ... })` — تُعرض كصندوق مميّز (خلفية زرقاء فاتحة، حدود). (ج) مؤشر Gauge إن وُجد أصلًا للأداة على الصفحة، بنفس البيانات (`valueLabel`/`caption` مطابقين لما يظهر on-page عبر `RatioGauge`).
- **معيار الحجم:** مؤشر الـGauge داخل PDF نصف‑دائري (لا شريط أفقي مسطّح) بنفس نسب `RatioGauge.tsx` on-page مضروبة في `scale = widthCss/240` — أي تكبير مستقبلي لحجم الخط/العناصر يُطبَّق بنفس المبدأ (نسخ نسب مُتحقَّق منها مسبقًا، لا اختراع نسب جديدة). حجم الخط بجداول/عناوين PDF (والطباعة المباشرة) كبير بما يكفي ليكون مريحًا فعليًا عند الطباعة الفعلية على A4 (18px+ للعناوين، 16px للجداول)، لا حجم شاشة عادي مباشر بلا تكبير.
- **معيار عدم التراكب/التقطيع (PDF عبر jsPDF):** أي عنصر ذاتي الاكتفاء (Gauge، صندوق "طريقة الحساب") يُعلَّم بـ`data-avoid-break="true"` ويُقاس موضعه فعليًا (`getBoundingClientRect`) قبل الترقيم، فتُدفَع الصفحة بأكمله لصفحة تالية جديدة بدل تقطيعه في المنتصف — نفس مبدأ تفادي تقطيع صفوف الجدول الطويل الموجود أصلًا بالكود، معمَّم الآن كآلية عامة (`AVOID_BREAK_SELECTOR`) لأي عنصر جديد يُضاف مستقبلًا لقالب PDF، لا الجدول الطويل فقط.
- **معيار مسار Print (`window.print()`):** ممنوع الاعتماد على `visibility: hidden` وحدها لإخفاء بقية الصفحة — هذا يُخفي بصريًا لكنه **لا يُصفّر ارتفاع العنصر**، فتُنتج صفحات فارغة كثيرة بعدد يطابق ارتفاع الصفحة الأصلية الكامل (تحقّق فعلي بتاريخ 2026-09-07: 10 صفحات، 9 منها فارغة). القاعدة الصحيحة المعتمدة: `display: none` عبر `body *:not(:has([data-print-area])):not([data-print-area]):not([data-print-area] *)` (يتطلب `:has()`، مدعوم في المتصفحات الحديثة المستهدفة) — هذا يُخفي كل شيء فعليًا (يُزال من التخطيط) عدا `[data-print-area]` وسلسلة أجداده المباشرة (المطلوبة فقط كحاويات تفريغ، لا لعرضها بصريًا). سلسلة الأجداد هذه (كبطاقة `SectionCard` الزرقاء التي يعيش زر التصدير داخلها) يجب تجريدها من مظهرها الزخرفي الخاص (خلفية، حدود، padding، ظل) عبر قاعدة `:has([data-print-area])` منفصلة — وإلا يظهر إطار/رأس ملوّن غريب حول محتوى الطباعة الفعلي (تحقّق فعلي: إطار أزرق كامل حول المحتوى قبل هذا الإصلاح). `[data-print-area]` نفسه يحتاج `width: 100%` صراحةً بعد إزالة `position:absolute;inset:0` القديمة (التي كانت تفرض العرض الكامل ضمنيًا) — وإلا يُطبَع بعرض ضيق موروث من موضعه الأصلي الضيق داخل رأس البطاقة.
- **فحص إلزامي قبل اعتبار أي تحسين PDF/طباعة مكتملًا:** لا يُكتفى بفحص الكود. يُنتَج ملف PDF فعلي (مسار "Download PDF" عبر Playwright ينقر الزر الفعلي ويلتقط حدث التنزيل، ومسار "Print" عبر `page.emulateMedia({media:"print"})` + `page.pdf()` الذي يُحاكي فعليًا ما يُنتجه `window.print()`)، يُحوَّل بـ`pdftoppm` (متوفر على الجهاز) إلى صور PNG، وتُفحص الصور فعليًا (لا افتراضًا) بحثًا عن: تقطيع عنصر عبر حد صفحة، تراكب نص/رسم، صفحات فارغة زائدة، عرض ضيق غير مبرَّر. يُكرَّر الفحص بلغتين على الأقل (إنجليزي، وعربي RTL) لأن أخطاء الطباعة غالبًا ما تظهر بشكل مختلف أو أوضح باتجاه RTL.
- **حالة التطبيق الأولى (تحقّق فعلي بتاريخ 2026-09-07، أداة Density Calculator):** أُصلح كل ما سبق فعليًا؛ اكتُشفت أثناء الفحص مشكلة إضافية غير متوقعة (صف "Result/النتيجة" مكرّر بمسار الطباعة فقط، بسبب `heroLabel`/`heroValue` تُعرَض يدويًا بالتوازي مع كونها أصلًا أول عنصر بمصفوفة `resultRows` المُمرَّرة) — أُصلحت بحذف التكرار (`resultRows.slice(1)`) بدل حذف الصف اليدوي، حفاظًا على استخدام الخصائص القائمة. **نفس هذا الخلل تحديدًا مؤكَّد الوجود بأدوات أخرى أيضًا (تحقّق فعلي على pH Calculator بعد إصلاح CSS المشترك)** — يُصلَح تلقائيًا كجزء من تعميم هذه القاعدة على بقية الأدوات، لا حاجة لإعادة اكتشافه من الصفر لكل أداة.
- **نطاق التطبيق المستقبلي:** أي أداة جديدة تُبنى بنمط `*ShareExportModal.tsx` القائم ترث تلقائيًا إصلاح `globals.css` (مشترك فعليًا)، لكن يجب التحقق يدويًا من: (أ) تمرير `sentence` فعليًا إلى `generateToolPdf`، (ب) عدم تكرار صف hero بقسم الطباعة اليدوي (استخدام `resultRows.slice(1)` أو حذف الصف اليدوي المكرر)، (ج) الفحص البصري الفعلي المذكور أعلاه قبل اعتبار الأداة مكتملة.

## 22. قاعدة معمارية دائمة: ممنوع ترك فراغ بصري حول أي رسم/مؤشر مفرد داخل بطاقته (2026-09-12)

هذه القاعدة تُطبَّق تلقائيًا على كل رسم/مؤشر تفاعلي أو ثابت (Gauge دائري، بوصلة، مخطط، شريط تقدّم، إلخ) في أي بطاقة (`SectionCard`/`EncyclopediaLiveWidget`) بأي أداة حالية أو مستقبلية، دون الحاجة لتكرار الطلب — تكمّل §17 (الفراغ حول عمود كامل above-the-fold، يُملأ بجدول مرجعي) لكنها تعالج مستوى أدق: الفراغ **داخل بطاقة رسم واحدة نفسها**، حين يكون الرسم صغيرًا نسبيًا مقارنة بحجم البطاقة أو بالبطاقات المجاورة له بنفس الصف.

- **المشكلة الأصلية:** أُضيفت 5 بطاقات رسم جديدة لأداة Weather Forecast (مؤشر الرطوبة، بوصلة الرياح، مؤشر UV، إلخ)، وبعضها (خصوصًا مؤشر الرطوبة والبوصلة) ترك مساحة بيضاء ملحوظة أسفل الرسم نفسه داخل حدود بطاقته، بينما البطاقات المجاورة بنفس الصف (كمخطط اتجاه الحرارة) كانت أطول بسبب محتواها الأغنى — فبدت البطاقات القصيرة "عائمة" بفراغ غير مستغل.
- **القاعدة:** أي بطاقة تحتوي رسمًا/مؤشرًا **تُفحص فعليًا** (لقطة شاشة حقيقية، لا افتراضًا من الكود) بحثًا عن فراغ ملحوظ حول الرسم أو أسفله داخل حدود البطاقة. إذا وُجد فراغ:
  - يُملأ بمحتوى نصي سياقي حقيقي ومرتبط مباشرة بالقيمة الفعلية المعروضة — جملة وصفية حية (مثال: "رياح معتدلة من الشمال الشرقي، مناسبة للأنشطة الخارجية")، تصنيف/نصيحة مرتبطة بالتصنيف الحالي (كما في مؤشر UV: نص وقائي يتغيّر حسب المستوى)، أو حقيقة/مقارنة ذات صلة — لا حشو زخرفي بلا معنى ولا مجرد تكبير للرسم نفسه بلا داعٍ.
  - إن كانت بطاقات متجاورة بنفس الصف (`grid`) متفاوتة الارتفاع بسبب هذا الفراغ، يُوسَّع محتوى البطاقة الأقصر (بنفس أسلوب النص السياقي أعلاه) ليقترب ارتفاعها من جاراتها، لا تُترك "تطفو" بفراغ غير مستغل ولا يُلجأ لحل بصري صناعي (كخلفية مزخرفة فارغة) بدل محتوى حقيقي.
- **حالة التطبيق الأولى (تحقّق فعلي بتاريخ 2026-09-12، أداة Weather Forecast):** أُضيفت جملة تصنيف/نصيحة حية أسفل مؤشرَي الرطوبة والرياح (بنفس نمط `uvGauge.advice.*` الذي كان موجودًا أصلًا لمؤشر UV)، وتحقّق فعليًا عبر لقطات شاشة أن الفراغ اختفى.
- **نطاق التطبيق المستقبلي:** عند إضافة أي رسم/مؤشر جديد لأي أداة (حالية أو مستقبلية)، **يُفحص فعليًا** بلقطة شاشة حقيقية بعد إضافته مباشرة — لا يُنتظر طلب منفصل لكل حالة — ويُملأ أي فراغ ملحوظ بنفس أسلوب المحتوى السياقي أعلاه قبل اعتبار الرسم مكتملًا.
