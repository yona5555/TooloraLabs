# خارطة الطريق — TooloraLabs

> **ملاحظة مهمة:** هذا الملف مرجع تصميم (design reference) مبني على تحليل موقع منافس/نموذج مشابه، الهدف منه تحديد الفجوات الوظيفية والهيكلية بيننا وبين نسخة ناضجة من هذا النوع من المواقع. هذا **ليس نسخًا حرفيًا** لذلك الموقع — الألوان، الخطوط، والتفاصيل البصرية تُبنى لاحقًا بما يناسب هوية TooloraLabs الخاصة، وليس تقليدًا حرفيًا للمرجع.

## الغرض من هذا الملف

يوثّق هذا الملف الفجوة بين الوضع الحالي للموقع والتصميم المرجعي المعتمد للنسخة النهائية، حتى يكون مرجعًا ثابتًا لتخطيط العمل القادم بدل الاعتماد على الذاكرة أو نقاشات متفرقة.

## الميزات المطلوبة (مرتبة حسب الأولوية المنطقية)

### 1. مبدّل اللغة الظاهر بالواجهة (زر en/ar بالـ Navbar)

- **الوضع الحالي:** بنية next-intl جاهزة بالكامل (routing، رسائل en/ar، وكل الصفحات والحاسبات مترجمة فعليًا)، لكن ما فيه أي زر ظاهر بالـ Navbar يخلي المستخدم يبدّل اللغة بنفسه — التبديل حاليًا فقط عبر رابط URL يدوي.
- **المطلوب:** زر أو قائمة صغيرة بـ `apps/web/components/layout/Navbar.tsx` تبدّل بين en/ar وتحافظ على نفس الصفحة الحالية (باستخدام `usePathname` / `useRouter` من `@/i18n/navigation`).
- **الأولوية:** الأسهل والأقرب للتنفيذ لأن البنية التحتية جاهزة أصلًا بالكامل.

### 2. مبدّل الوضع الليلي/النهاري (Dark/Light mode)

- **الوضع الحالي:** لا يوجد أي دعم لـ dark mode في المشروع — لا كلاسات Tailwind من نوع `dark:`، ولا آلية تبديل أو تخزين تفضيل.
- **المطلوب:** زر تبديل بالـ Navbar، دعم `dark:` عبر مكوّنات الموقع (خصوصًا الحاسبات السبع الحالية وصفحات الأدوات/الفئات)، مع حفظ تفضيل المستخدم.

### 3. توسيع الفئات من 6 إلى 12 فئة

- **الوضع الحالي:** 6 فئات معرّفة في `apps/web/data/categories.ts`: Calculators, Converters, AI Tools, Developer Tools, Text Tools, File Tools.
- **المطلوب:** 12 فئة إجمالًا — الإبقاء على الأربع الأساسية (Calculators, Converters, AI Tools, Developer Tools) كما هي، وإضافة: Text Tools, File Tools, Image Tools, SEO Tools, Finance Tools, Health Tools, Unit Converters, Generators.
- **ملاحظة مهمة:** بعض هذي الفئات موجود مضمونها فعليًا اليوم لكن تحت مسمّيات/تصنيفات مختلفة (مثلًا "Unit Converter" أداة واحدة مصنّفة حاليًا تحت Converters، وحاسبات الرهن العقاري والضريبة والخصم والإكرامية مصنّفة كلها تحت Calculators العامة). المطلوب توحيد التصنيف وإعادة ترتيبه ضمن الفئات الجديدة، وليس إضافة فئات فارغة موازية للموجود.

### 4. نظام "الأدوات الشائعة" (Popular Tools) بتقييمات نجوم وعدد استخدام

- **الوضع الحالي:** يوجد قسم "Featured Tools" ثابت (`apps/web/components/tools/FeaturedTools.tsx`) يعرض فقط الأدوات المعلّمة يدويًا بـ `featured: true` في `data/tools.ts`، بدون أي تقييم نجوم أو عدّاد استخدام حقيقي.
- **المطلوب:** حقل تقييم (نجوم) وعدد استخدام فعلي لكل أداة، مع منطق حقيقي لحسابهما وتحديثهما — يحتاج تخزين بيانات فعلي (قاعدة بيانات أو ما يعادلها)، وليس أرقامًا ثابتة بالكود.

### 5. زر "Sign In" ونظام مستخدمين

- **الوضع الحالي:** زر "Sign In" موجود بالـ Navbar لكنه شكلي بالكامل، بدون أي وظيفة أو صفحة تسجيل دخول مرتبطة.
- **المطلوب:** نظام تسجيل دخول وحسابات مستخدمين حقيقي.
- **ملاحظة:** هذي الميزة تُبنى **لاحقًا**، بعد استقرار الأدوات الأساسية — ليست أولوية في المرحلة الحالية.

### 6. صفحة Blog

- **الوضع الحالي:** غير موجودة إطلاقًا في المشروع.
- **المطلوب:** بناء قسم مدونة كامل (قائمة مقالات + صفحة عرض مقال منفرد)، مترجم en/ar بنفس نمط بقية الموقع.

### 7. صفحتا About وContact حقيقيتين

- **الوضع الحالي:** كانتا مجلدين فارغين بدون أي محتوى فعلي، وتمّت إزالتهما سابقًا لتجنّب صفحات فارغة منشورة.
- **المطلوب:** بناء المحتوى الفعلي لهاتين الصفحتين لاحقًا (محتوى حقيقي، وليس مجرد مجلد Route فارغ).

### 8. قسم "What's New" الجانبي

- **الوضع الحالي:** غير موجود.
- **المطلوب:** قسم جانبي (widget) يعرض آخر الإضافات أو التحديثات على الموقع.

### 9. توسيع عدد الأدوات الفعلية

- **الوضع الحالي (محدَّث 2026-08-07):**
  - **22 أداة فعلية وشغّالة بالكامل** (مترجمة en/ar، ومربوطة فعليًا بمحرك `packages/tools` حيث ينطبق) — **لا توجد أي أداة "Coming Soon" متبقية بالموقع حاليًا**، بما فيها الخمس المذكورة سابقًا هنا كغير منفَّذة (محول الصور، منسّق JSON، مولّد كلمات المرور، محول الوحدات، مولّد رمز QR — كلها منفَّذة وشغّالة الآن).
  - موزَّعة على 6 من الفئات الـ12 المستهدفة: Calculators (11 أداة)، File Tools (4)، Developer Tools (3)، Converters (2)، AI Tools (1)، Text Tools (1).
  - ثلاث منها فقط (حاسبة العمر، BMI، الرهن العقاري) بمستوى العمق الكامل حسب §0 و§8 بملف التعليمات (above the fold ثلاثي الأعمدة + قسم موسوعي كامل)؛ الباقي أدوات أبسط بلا قسم موسوعي بعد (عمل لاحق أوسع، لا يمنع كونها شغّالة وظيفيًا).
- **المطلوب:** (1) توسيع الفئات الـ6 المتبقية من أصل 12 (Image Tools، SEO Tools، Finance Tools، Health Tools، Unit Converters كفئة مستقلة، Generators) بأدوات فعلية فيها، و(2) رفع الأدوات الـ19 البسيطة الحالية تدريجيًا لنفس مستوى العمق (above the fold + قسم موسوعي) المطبَّق على الثلاث الأولى — لا مجرد إضافة أسماء أدوات جديدة إلى `data/tools.ts`.

### 10. قيد مؤقت: نافذة الرسم البياني التاريخي بقسم السلع (Commodities)

- **الوضع الحالي (2026-08-10):** قسم السلع (`commodities-tracker`) يستخدم نافذة تاريخية ثابتة بـ7 أيام فقط لرسم سعر الذهب، بلا اختيار فترة (1شهر/1سنة) كما في أداتَي العملات الرقمية والعملات الدولية.
- **السبب:** خطة MetalpriceAPI المجانية (مصدر بيانات الذهب/الفضة) لا توفر endpoint نطاق تاريخي (Timeframe) إلا بخطة مدفوعة، والبديل الوحيد المتاح (طلب لكل تاريخ على حدة) سيستهلك عشرات الطلبات من أصل 100 طلب/شهر فقط لبناء نافذة 30 يومًا. جُرِّبت بدائل مجانية أخرى (FRED — تبيّن أن سلسلة الذهب اليومية أُلغيت فعليًا من المنصة؛ Stooq — محجوب خلف فحص JavaScript لا يعمل من الخادم؛ Nasdaq Data Link — محجوب خلف حماية Incapsula) ولم تنجح أي منها.
- **الحل الحالي:** نافذة 7 أيام ثابتة فقط، بتخزين طويل الأمد لكل تاريخ ماضٍ (لا يتغيّر أبدًا بعد نشره)، مما يبقي الاستهلاك الشهري ضمن الحد المجاني بأمان.
- **المطلوب لاحقًا:** يُعاد تقييم هذا القيد بعد الإطلاق الفعلي وتوفر بيانات زيارات حقيقية على قسم السلع، لترقية خطة MetalpriceAPI (أو اعتماد مصدر بديل مدفوع) إن استدعى الأمر لدعم فترات أطول (شهر/سنة) بنفس عمق أداتَي العملات.

### 11. أداة جديدة: فرق التوقيت والمناطق الزمنية (World Time / Time Zone Converter)

- **الوضع الحالي (2026-08-10):** طُلبت هذه الأداة كخطوة تالية بعد اكتمال TDEE، الفائدة المركبة، وحاسبة القروض — بنفس منهجية BMI ونفس عمق تلك الأدوات الثلاث (above-the-fold ثلاثي الأعمدة + قسم موسوعي كامل A4 + SectionNav لاصق + AdSpace + محتوى عربي فصيح كامل).
- **الطلب الأصلي:** استخدام **WorldTime API** (worldtimeapi.org) كمصدر بيانات.
- **نتيجة بحث فعلي (2026-08-10) قبل البدء بأي كود:**
  - **WorldTimeAPI (worldtimeapi.org) غير موثوق تشغيليًا**: أكثر من 195 انقطاعًا (outage) خلال آخر 4 أشهر حسب مراقب حالة خارجي (StatusGator)، بالإضافة لتقارير مستخدمين متكررة عن تعطّل الخدمة ووجود rate-limiting صارم — مصدر غير آمن للبناء عليه كميزة دائمة بالموقع.
  - **المفارقة المعمارية:** طلب المستخدم صراحةً "بنفس منهجية BMI" — ومنهجية BMI في هذا المشروع تعني تحديدًا **حساب خالص بدون أي API خارجي** (بعكس أدوات مثل forex-converter وcommodities-tracker وweather-forecast المصنّفة كأدوات "بيانات حية" ومُستثناة من `generateStaticParams`). حساب الوقت الحالي/فرق التوقيت لأي منطقة زمنية IANA هو عملية **حتمية بالكامل** يمكن حسابها محليًا (client-side) عبر `Intl.DateTimeFormat` القياسي في JavaScript، بدون أي اتصال شبكة، وبدون أي حد استخدام (rate limit)، وبموثوقية 100% — وهذا يطابق تمامًا منهجية BMI الفعلية (حساب خالص)، ويناقض الاعتماد على WorldTimeAPI غير المستقر.
  - **البديل المتاح إن رغب المستخدم بمصدر خارجي رغم ذلك:** TimeAPI.io أو TimeZoneDB (بديلان أكثر استقرارًا حسب نفس البحث)، لكن كلاهما يُدخل الأداة في تصنيف "بيانات حية" (استثناء من SSG، مراجعة ToS، إلخ) بلا داعٍ حقيقي بما أن البيانات حتمية وليست فعليًا "حية" بالمعنى الذي يتطلب مصدرًا خارجيًا (كسعر صرف أو حالة طقس).
- **القرار:** **معلَّق بانتظار اختيار المستخدم صراحةً** بين: (أ) حساب خالص بـ`Intl.DateTimeFormat` بنفس منهجية BMI الحرفية (موصى به)، أو (ب) الاستمرار بربط WorldTimeAPI رغم عدم استقراره، أو (ج) استخدام مصدر بديل أكثر استقرارًا (TimeAPI.io/TimeZoneDB) كأداة "بيانات حية" مصنّفة ضمن استثناءات SSG.
- **لن يبدأ أي كود فعلي قبل حسم هذا القرار.**

## خطة تطبيق الموبايل (مستقبلي)

### الهدف

بعد ما يستقر موقع الويب، نشر تطبيق على **Apple App Store** و**Google Play Store**، على الأرجح باستخدام **React Native** أو **Expo**. الفكرة الأساسية: إعادة استخدام منطق `packages/core` و`packages/sdk` و`packages/tools` **مباشرة وبدون تعديل**، لأنها TypeScript خالص بدون أي اعتماد على DOM أو المتصفح — والاكتفاء ببناء واجهة موبايل جديدة فوقها فقط (مو إعادة كتابة منطق الحسابات من الصفر).

### القيد المعماري — يبدأ الالتزام به من الآن

هذا مو خطة مستقبلية بس، هذا قاعدة تنطبق على كل شغل قادم بالمشروع:

> **`packages/core` و`packages/sdk` و`packages/tools` يجب أن تبقى "منطق خالص" (pure logic) — بدون أي اعتماد على DOM أو `window` أو `document` أو `localStorage` أو أي API خاص بالمتصفح. يجب أن تشتغل في أي بيئة JavaScript (متصفح، React Native، Node.js) بدون أي تعديل.**

أي كود يحتاج بيئة المتصفح تحديدًا (حفظ تفضيل بـ`localStorage`، قراءة `document`، أي تفاعل مع DOM) يبقى محصورًا داخل `apps/web` فقط، ولا ينزل أبدًا لهذي الحزم الثلاث.

### نتيجة المراجعة (تمّت اليوم)

راجعت الكود الحالي بالكامل بالحزم الثلاث بحثًا عن أي مخالفة لهذا القيد — **ما لقيت أي مخالفة**:

- بحث شامل عن `window`، `document`، `localStorage`، `sessionStorage`، `navigator` بمجلدات `src` الثلاث: **لا يوجد أي استخدام**.
- الدوال المضافة اليوم تحديدًا (`detectDigitStyle`، `formatLocalizedNumber` في `packages/core`) تستخدم فقط regex عادي و`Intl.NumberFormat` — وهذي ليست API خاصة بالمتصفح، بل جزء من مواصفة JavaScript القياسية (ECMA-402) وموجودة في Node.js وReact Native (عبر محرك Hermes) على حد سواء.
- كل منطق التحقق (validation) اللي أضفناه اليوم بحدود الحاسبات السبع موجود بالكامل داخل `apps/web/components/tools/*` — مو داخل `packages/tools`. الحزمة المشتركة نفسها ما فيها أي حدود تحقق مكتوبة، فقط التنفيذ الحسابي الخالص.
- **ملاحظة إضافية مهمة:** ملف `tsconfig.base.json` (اللي ترثه الحزم الثلاث) يحدد `"lib": ["ES2022"]` فقط — بدون `"DOM"`. يعني حتى لو حاول أحد بالمستقبل يستخدم `window`/`document` بالخطأ داخل هذي الحزم، TypeScript نفسه بيرفض يبني الكود لأن أنواع DOM غير معرّفة أصلًا بهذا الإعداد. هذا قيد مفروض تلقائيًا على مستوى الأدوات، مو بس اتفاق شفهي.
