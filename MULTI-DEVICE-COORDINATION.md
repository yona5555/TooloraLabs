# بروتوكول تنسيق العمل بين الأجهزة — TooloraLabs

## السياق

جهاز الماك ميني هو الجهة المنسِّقة والموجِّهة للعمل التقني. جهاز اللينكس (Session 1 - Worker) جلسة مساعِدة تنفّذ التوجيهات ولا تتخذ قرارات هيكلية مستقلة. صاحب المشروع هو السلطة العليا فوق الجهازين معًا.

## حوادث حدثت فعليًا بسبب غياب التنسيق (يجب تفاديها)

1. فقدان أسطر exports مهمة من `packages/tools/src/calculators/index.ts` أثناء إضافة أداة من جهاز، كسر الـbuild بالكامل لكل من سحب المشروع لاحقًا.
2. تعارض تصنيف فئات (category) بين إعادة هيكلة فئات على جهاز وإضافة أدوات بفئة قديمة محذوفة من جهاز آخر، تسبب في أخطاء MISSING_MESSAGE.
3. تراكم عشرات الـcommits غير المتزامنة بين الجهازين، احتاج ساعات لحل تعارضاتها يدويًا.

## هيكل المسؤولية بين الجلسات

**جهاز الماك ميني = الجهة المنسِّقة والموجِّهة:**

- يقرر ويوجّه توزيع المهام بين الجلسات، يصدر التوجيهات التي ينفّذها اللينكس.
- يتولى رفع الأدوات لمستوى الجودة المعمارية الكاملة بعد أن يبنيها اللينكس وظيفيًا.
- القرار النهائي في أي تعارض يعود له، بالتشاور مع صاحب المشروع.

**جهاز اللينكس (Session 1 - Worker) = جلسة مساعِدة تنفّذ التوجيهات:**

- يبني الأدوات الجديدة (منطق الحساب، الواجهة الأساسية، الترجمة) حسب التوجيه المستلم.
- لا يتخذ قرارات هيكلية مستقلة دون توجيه صريح.
- ينفّذ git pull قبل أي عمل، ويتحقق من عدم وجود عمل متزامن قبل البدء.
- يدفع (commit + push) بشكل متكرر بعد كل أداة مكتملة.

**صاحب المشروع = السلطة العليا فوق الجهازين معًا.**

## القواعد الإلزامية لأي جلسة

1. `git pull origin main` فورًا قبل أي عمل، في بداية كل جلسة.
2. تحقق من `git status` و`git log` للتأكد من عدم وجود عمل متزامن على نفس الملفات.
3. عند وجود عمل متزامن محتمل التعارض، توقف وأخبر صاحب المشروع قبل المتابعة.
4. commit وpush متكرر وسريع بعد كل جزء منجز.
5. حذر خاص عند تعديل ملفات مشتركة حساسة (`index.ts`، `data/tools.ts`، ملفات التصنيفات) — راجع الـdiff الكامل قبل أي commit يلمسها.

## ربط الأدوات بالقواعد المعمارية المحفوظة مسبقًا

أي أداة قيد البناء يجب أن تلتزم لاحقًا (عبر تعميق الماك) بالقواعد الموثّقة في `TooloraLabs-Claude-Instructions.md` و`ROADMAP.md`، من ضمنها: رسم توضيحي (SVG) موسوم بالمتغيرات لكل أداة، لا فراغات بصرية، أزرار Calculate/Clear، مؤشر بصري لكل نتيجة رئيسية، نافذة Share/Export كاملة، لا صناديق تحذير ملوّنة.
